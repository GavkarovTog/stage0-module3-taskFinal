package lang.print.gaps.finalModuleTask;

public class DigitsSumCalculator {
    public void calculateSum(int number) {
        String strRepr = number + "";

        System.out.println(strRepr.charAt(0) + strRepr.charAt(1) + strRepr.charAt(2) + strRepr.charAt(3));
    }
}
